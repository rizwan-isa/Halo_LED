# Project configuration consumed by GitHub Actions and local `act`.
# This file is updated by scripts/init_project.py and the "Init Project" workflow.
#
# BaseFileName should match the KiCad project stem (e.g. <BaseFileName>.kicad_pcb).
BaseFileName=LED

# Your internal identifiers (used in README replacements, filenames, etc.)
PCBANumber=ES-A00003
PCBNumber=ES-000003
